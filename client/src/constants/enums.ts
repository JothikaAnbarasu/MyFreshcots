export enum EUserType {
  Candidate = "candidate",
  Volunteer = "volunteer",
  Employer = "employer",
}

export enum EEmployerCategory {
  Internship = "internship",
  Employment = "employment",
}

export enum EModalName {
  SubscriptionPopup = "subscription-popup",
  UnderContructionPopup = "underConstruction-popup",
}

export enum EContactChangeType {
  Email = "email",
  Phone = "phone",
}
